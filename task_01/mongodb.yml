apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: lms-pvc
  namespace: lms-app-namespace
spec:
  resources:
    requests:
      storage: 2Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
